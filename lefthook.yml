pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm lint -- {staged_files}
    format:
      glob: "*.{js,jsx,ts,tsx,json,css}"
      run: pnpm fmt -- {staged_files}

pre-push:
  commands:
    check:
      run: pnpm check
