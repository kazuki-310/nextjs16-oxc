# 従業員登録

## 目的

新しい従業員（`employees`）レコードを作成し、登録直後にログインセッションを開始する。

## 要件

- ニックネームまたはメールアドレスとパスワードでアカウントを作成できる
- 既に同一のメールまたはニックネームが存在する場合は登録できない
- 登録成功後、認証済み状態でトップ（`/`）へ遷移する

## 対応ページ

- `apps/web/src/app/register/page.tsx`
