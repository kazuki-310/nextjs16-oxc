# 認証（ログイン）

## 目的

ログイン済みユーザーのみが保護ルートにアクセスできるようにする。ニックネームまたはメールアドレスとパスワードで認証する。

## 要件

- ニックネームまたはメールアドレスとパスワードでログインできる
- ログイン失敗が一定回数に達した場合、アカウントを一時ロックする

## 対応ページ

- `apps/web/src/app/login/page.tsx`

## 関連

- 新規アカウント作成は [従業員登録](../register/overview.md)（`/register`）を参照する。
